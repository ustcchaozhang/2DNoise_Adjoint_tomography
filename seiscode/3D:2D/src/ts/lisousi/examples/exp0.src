Isotropic source (explosion) at topmost interface (normally surface)

typ, sig, src, Ts, Td, Zs
1, 1, 1, 0., 0.0125, 0.000

M0, Mxx, Myy, Mzz, Mxy, Mxz, Myz
1.e13, 1., 1., 1., 0., 0., 0.

units
nm

sourec is defined as
vertical single force (typ=2)
source signal is taken as is (sig=2), 
  i.e. first derivative of Müller-Brüstle function,
  i.e. sin(t)**3
use compiled source time function (src=1)
